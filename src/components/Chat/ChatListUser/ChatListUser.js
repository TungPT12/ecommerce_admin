import React from 'react';
import styles from './ChatListUser.module.css'

function ChatListUser() {
    return (
        <div className={`bg-white w-100 h-100 ${styles['chat-list-user']}`}>
            list user
        </div>
    );
}

export default ChatListUser;